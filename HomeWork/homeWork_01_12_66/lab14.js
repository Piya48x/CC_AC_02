let facts = { numPlanets: 8, yearNeptuneDiscovered: 1846 };
let { numPlanets, yearNeptuneDiscovered } = facts;
console.log(numPlanets); // *8
console.log(yearNeptuneDiscovered); // **1846
/**numPlanets มีค่าเป็น 8 ซึ่งเป็นค่าที่ถูกดึงมาจาก property numPlanets ใน Object facts.
yearNeptuneDiscovered มีค่าเป็น 1846 ซึ่งเป็นค่าที่ถูกดึงมาจาก property yearNeptuneDiscovered ใน Object facts. */